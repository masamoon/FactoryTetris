# Gridforge development

Read [AGENTS.md](AGENTS.md) for current commands, architecture, and simulation invariants. The default runtime is the side-view Asteroid Works prototype. The earlier foldable tile workshop remains available at `?mode=tiles`.
